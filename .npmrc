# openapi-typescript (manual `gen:api` codegen tool) still peer-caps TypeScript at
# ^5.x while the app is on TypeScript 6. The mismatch is only in that dev tool's
# declared peer range, not at runtime, so we relax strict peer resolution here.
# Respected by both `npm install` and the container's `npm ci`.
legacy-peer-deps=true
