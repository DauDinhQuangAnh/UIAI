import { defineConfig, devices } from "@playwright/test";

// E2E runs against the Vite dev server with the API fully mocked at the network
// layer (page.route) inside each spec — deterministic, no backend dependency. The
// dev server proxies /api, but every /api call is intercepted before it leaves.
export default defineConfig({
  testDir: "./e2e",
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 1 : 0,
  reporter: process.env.CI ? "github" : "list",
  use: {
    baseURL: "http://localhost:5173",
    trace: "on-first-retry",
  },
  projects: [{ name: "chromium", use: { ...devices["Desktop Chrome"] } }],
  webServer: {
    command: "npm run dev",
    url: "http://localhost:5173",
    reuseExistingServer: !process.env.CI,
    timeout: 120_000,
  },
});
