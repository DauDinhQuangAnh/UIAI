# Dev-only. In production the SPA is served same-origin (go:embed) and calls /api
# on its own origin, so no base URL is needed. In dev, Vite proxies /api to this.
VITE_API_BASE_URL=http://localhost:8080
