# Optional browser base URL. Leave empty in dev so Vite can proxy /api and avoid CORS.
# VITE_API_BASE_URL=

# Dev-server proxy target for /api.
API_PROXY_TARGET=https://sar-be.onrender.com

# Meta OAuth client-driven flow (section 6).
VITE_META_APP_ID=
VITE_META_APP_SECRET=
VITE_META_OAUTH_CALLBACK_URL=
